//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Requests
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class ListRefundsMetadataType
    {
        private ListRefundsMetadataType(string value) { Value = value; }

        public string Value { get; private set; }

        public static ListRefundsMetadataType Str { get { return new ListRefundsMetadataType("str"); } }

        public static ListRefundsMetadataType MapOfAny { get { return new ListRefundsMetadataType("mapOfAny"); } }

        public static ListRefundsMetadataType ArrayOfStr { get { return new ListRefundsMetadataType("arrayOfStr"); } }

        public static ListRefundsMetadataType Null { get { return new ListRefundsMetadataType("null"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(ListRefundsMetadataType v) { return v.Value; }
        public static ListRefundsMetadataType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "mapOfAny": return MapOfAny;
                case "arrayOfStr": return ArrayOfStr;
                case "null": return Null;
                default: throw new ArgumentException("Invalid value for ListRefundsMetadataType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((ListRefundsMetadataType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }


    /// <summary>
    /// Provide any data you like, for example a string or a JSON object. We will save the data alongside the entity. Whenever<br/>
    /// 
    /// <remarks>
    /// you fetch the entity with our API, we will also include the metadata. You can use up to approximately 1kB.
    /// </remarks>
    /// </summary>
    [JsonConverter(typeof(ListRefundsMetadata.ListRefundsMetadataConverter))]
    public class ListRefundsMetadata
    {
        public ListRefundsMetadata(ListRefundsMetadataType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public string? Str { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public Dictionary<string, object>? MapOfAny { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public List<string>? ArrayOfStr { get; set; }

        public ListRefundsMetadataType Type { get; set; }
        public static ListRefundsMetadata CreateStr(string str)
        {
            ListRefundsMetadataType typ = ListRefundsMetadataType.Str;

            ListRefundsMetadata res = new ListRefundsMetadata(typ);
            res.Str = str;
            return res;
        }
        public static ListRefundsMetadata CreateMapOfAny(Dictionary<string, object> mapOfAny)
        {
            ListRefundsMetadataType typ = ListRefundsMetadataType.MapOfAny;

            ListRefundsMetadata res = new ListRefundsMetadata(typ);
            res.MapOfAny = mapOfAny;
            return res;
        }
        public static ListRefundsMetadata CreateArrayOfStr(List<string> arrayOfStr)
        {
            ListRefundsMetadataType typ = ListRefundsMetadataType.ArrayOfStr;

            ListRefundsMetadata res = new ListRefundsMetadata(typ);
            res.ArrayOfStr = arrayOfStr;
            return res;
        }

        public static ListRefundsMetadata CreateNull()
        {
            ListRefundsMetadataType typ = ListRefundsMetadataType.Null;
            return new ListRefundsMetadata(typ);
        }

        public class ListRefundsMetadataConverter : JsonConverter
        {

            public override bool CanConvert(System.Type objectType) => objectType == typeof(ListRefundsMetadata);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                var json = JRaw.Create(reader).ToString();
                if (json == "null")
                {
                    return null;
                }

                var fallbackCandidates = new List<(System.Type, object, string)>();

                if (json[0] == '"' && json[^1] == '"'){
                    return new ListRefundsMetadata(ListRefundsMetadataType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                try
                {
                    return new ListRefundsMetadata(ListRefundsMetadataType.MapOfAny)
                    {
                        MapOfAny = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<Dictionary<string, object>>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(Dictionary<string, object>), new ListRefundsMetadata(ListRefundsMetadataType.MapOfAny), "MapOfAny"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                try
                {
                    return new ListRefundsMetadata(ListRefundsMetadataType.ArrayOfStr)
                    {
                        ArrayOfStr = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<List<string>>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(List<string>), new ListRefundsMetadata(ListRefundsMetadataType.ArrayOfStr), "ArrayOfStr"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null) {
                    writer.WriteRawValue("null");
                    return;
                }

                ListRefundsMetadata res = (ListRefundsMetadata)value;
                if (ListRefundsMetadataType.FromString(res.Type).Equals(ListRefundsMetadataType.Null))
                {
                    writer.WriteRawValue("null");
                    return;
                }

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.MapOfAny != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.MapOfAny));
                    return;
                }

                if (res.ArrayOfStr != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.ArrayOfStr));
                    return;
                }
            }

        }

    }
}