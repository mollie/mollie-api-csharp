//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Requests
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using Newtonsoft.Json.Linq;
    using System;
    using System.Collections.Generic;
    using System.Numerics;
    using System.Reflection;

    public class GetPaymentMetadataType
    {
        private GetPaymentMetadataType(string value) { Value = value; }

        public string Value { get; private set; }

        public static GetPaymentMetadataType Str { get { return new GetPaymentMetadataType("str"); } }

        public static GetPaymentMetadataType MapOfAny { get { return new GetPaymentMetadataType("mapOfAny"); } }

        public static GetPaymentMetadataType ArrayOfStr { get { return new GetPaymentMetadataType("arrayOfStr"); } }

        public static GetPaymentMetadataType Null { get { return new GetPaymentMetadataType("null"); } }

        public override string ToString() { return Value; }
        public static implicit operator String(GetPaymentMetadataType v) { return v.Value; }
        public static GetPaymentMetadataType FromString(string v) {
            switch(v) {
                case "str": return Str;
                case "mapOfAny": return MapOfAny;
                case "arrayOfStr": return ArrayOfStr;
                case "null": return Null;
                default: throw new ArgumentException("Invalid value for GetPaymentMetadataType");
            }
        }
        public override bool Equals(object? obj)
        {
            if (obj == null || GetType() != obj.GetType())
            {
                return false;
            }
            return Value.Equals(((GetPaymentMetadataType)obj).Value);
        }

        public override int GetHashCode()
        {
            return Value.GetHashCode();
        }
    }


    /// <summary>
    /// Provide any data you like, for example a string or a JSON object. We will save the data alongside the entity. Whenever<br/>
    /// 
    /// <remarks>
    /// you fetch the entity with our API, we will also include the metadata. You can use up to approximately 1kB.
    /// </remarks>
    /// </summary>
    [JsonConverter(typeof(GetPaymentMetadata.GetPaymentMetadataConverter))]
    public class GetPaymentMetadata
    {
        public GetPaymentMetadata(GetPaymentMetadataType type)
        {
            Type = type;
        }

        [SpeakeasyMetadata("form:explode=true")]
        public string? Str { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public Dictionary<string, object>? MapOfAny { get; set; }

        [SpeakeasyMetadata("form:explode=true")]
        public List<string>? ArrayOfStr { get; set; }

        public GetPaymentMetadataType Type { get; set; }
        public static GetPaymentMetadata CreateStr(string str)
        {
            GetPaymentMetadataType typ = GetPaymentMetadataType.Str;

            GetPaymentMetadata res = new GetPaymentMetadata(typ);
            res.Str = str;
            return res;
        }
        public static GetPaymentMetadata CreateMapOfAny(Dictionary<string, object> mapOfAny)
        {
            GetPaymentMetadataType typ = GetPaymentMetadataType.MapOfAny;

            GetPaymentMetadata res = new GetPaymentMetadata(typ);
            res.MapOfAny = mapOfAny;
            return res;
        }
        public static GetPaymentMetadata CreateArrayOfStr(List<string> arrayOfStr)
        {
            GetPaymentMetadataType typ = GetPaymentMetadataType.ArrayOfStr;

            GetPaymentMetadata res = new GetPaymentMetadata(typ);
            res.ArrayOfStr = arrayOfStr;
            return res;
        }

        public static GetPaymentMetadata CreateNull()
        {
            GetPaymentMetadataType typ = GetPaymentMetadataType.Null;
            return new GetPaymentMetadata(typ);
        }

        public class GetPaymentMetadataConverter : JsonConverter
        {

            public override bool CanConvert(System.Type objectType) => objectType == typeof(GetPaymentMetadata);

            public override bool CanRead => true;

            public override object? ReadJson(JsonReader reader, System.Type objectType, object? existingValue, JsonSerializer serializer)
            {
                var json = JRaw.Create(reader).ToString();
                if (json == "null")
                {
                    return null;
                }

                var fallbackCandidates = new List<(System.Type, object, string)>();

                if (json[0] == '"' && json[^1] == '"'){
                    return new GetPaymentMetadata(GetPaymentMetadataType.Str)
                    {
                        Str = json[1..^1]
                    };
                }

                try
                {
                    return new GetPaymentMetadata(GetPaymentMetadataType.MapOfAny)
                    {
                        MapOfAny = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<Dictionary<string, object>>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(Dictionary<string, object>), new GetPaymentMetadata(GetPaymentMetadataType.MapOfAny), "MapOfAny"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                try
                {
                    return new GetPaymentMetadata(GetPaymentMetadataType.ArrayOfStr)
                    {
                        ArrayOfStr = ResponseBodyDeserializer.DeserializeUndiscriminatedUnionMember<List<string>>(json)
                    };
                }
                catch (ResponseBodyDeserializer.MissingMemberException)
                {
                    fallbackCandidates.Add((typeof(List<string>), new GetPaymentMetadata(GetPaymentMetadataType.ArrayOfStr), "ArrayOfStr"));
                }
                catch (ResponseBodyDeserializer.DeserializationException)
                {
                    // try next option
                }
                catch (Exception)
                {
                    throw;
                }

                if (fallbackCandidates.Count > 0)
                {
                    fallbackCandidates.Sort((a, b) => ResponseBodyDeserializer.CompareFallbackCandidates(a.Item1, b.Item1, json));
                    foreach(var (deserializationType, returnObject, propertyName) in fallbackCandidates)
                    {
                        try
                        {
                            return ResponseBodyDeserializer.DeserializeUndiscriminatedUnionFallback(deserializationType, returnObject, propertyName, json);
                        }
                        catch (ResponseBodyDeserializer.DeserializationException)
                        {
                            // try next fallback option
                        }
                        catch (Exception)
                        {
                            throw;
                        }
                    }
                }

                throw new InvalidOperationException("Could not deserialize into any supported types.");
            }

            public override void WriteJson(JsonWriter writer, object? value, JsonSerializer serializer)
            {
                if (value == null) {
                    writer.WriteRawValue("null");
                    return;
                }

                GetPaymentMetadata res = (GetPaymentMetadata)value;
                if (GetPaymentMetadataType.FromString(res.Type).Equals(GetPaymentMetadataType.Null))
                {
                    writer.WriteRawValue("null");
                    return;
                }

                if (res.Str != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.Str));
                    return;
                }

                if (res.MapOfAny != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.MapOfAny));
                    return;
                }

                if (res.ArrayOfStr != null)
                {
                    writer.WriteRawValue(Utilities.SerializeJSON(res.ArrayOfStr));
                    return;
                }
            }

        }

    }
}