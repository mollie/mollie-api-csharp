//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Requests
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using System;
    
    /// <summary>
    /// You can retrieve reports in two different formats. With the `status-balances` format, transactions are grouped<br/>
    /// 
    /// <remarks>
    /// by status (e.g. `pending`, `available`), then by transaction type, and then by other sub-groupings where<br/>
    /// available (e.g. payment method).<br/>
    /// <br/>
    /// With the `transaction-categories` format, transactions are grouped by<br/>
    /// transaction type, then by status, and then again by other sub-groupings where available.
    /// </remarks>
    /// </summary>
    public enum QueryParamGrouping
    {
        [JsonProperty("status-balances")]
        StatusBalances,
        [JsonProperty("transaction-categories")]
        TransactionCategories,
    }

    public static class QueryParamGroupingExtension
    {
        public static string Value(this QueryParamGrouping value)
        {
            return ((JsonPropertyAttribute)value.GetType().GetMember(value.ToString())[0].GetCustomAttributes(typeof(JsonPropertyAttribute), false)[0]).PropertyName ?? value.ToString();
        }

        public static QueryParamGrouping ToEnum(this string value)
        {
            foreach(var field in typeof(QueryParamGrouping).GetFields())
            {
                var attributes = field.GetCustomAttributes(typeof(JsonPropertyAttribute), false);
                if (attributes.Length == 0)
                {
                    continue;
                }

                var attribute = attributes[0] as JsonPropertyAttribute;
                if (attribute != null && attribute.PropertyName == value)
                {
                    var enumVal = field.GetValue(null);

                    if (enumVal is QueryParamGrouping)
                    {
                        return (QueryParamGrouping)enumVal;
                    }
                }
            }

            throw new Exception($"Unknown value {value} for enum QueryParamGrouping");
        }
    }

}