//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Components
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;

    /// <summary>
    /// The frequency with which the available amount on the balance will be settled to the configured transfer<br/>
    /// destination.<br/>
    /// <br/>
    /// Settlements created during weekends or on bank holidays will take place on the next business day.
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class ListEntityBalanceTransferFrequency : IEquatable<ListEntityBalanceTransferFrequency>
    {
        public static readonly ListEntityBalanceTransferFrequency EveryDay = new ListEntityBalanceTransferFrequency("every-day");
        public static readonly ListEntityBalanceTransferFrequency Daily = new ListEntityBalanceTransferFrequency("daily");
        public static readonly ListEntityBalanceTransferFrequency EveryMonday = new ListEntityBalanceTransferFrequency("every-monday");
        public static readonly ListEntityBalanceTransferFrequency EveryTuesday = new ListEntityBalanceTransferFrequency("every-tuesday");
        public static readonly ListEntityBalanceTransferFrequency EveryWednesday = new ListEntityBalanceTransferFrequency("every-wednesday");
        public static readonly ListEntityBalanceTransferFrequency EveryThursday = new ListEntityBalanceTransferFrequency("every-thursday");
        public static readonly ListEntityBalanceTransferFrequency EveryFriday = new ListEntityBalanceTransferFrequency("every-friday");
        public static readonly ListEntityBalanceTransferFrequency Monthly = new ListEntityBalanceTransferFrequency("monthly");
        public static readonly ListEntityBalanceTransferFrequency RevenueDay = new ListEntityBalanceTransferFrequency("revenue-day");
        public static readonly ListEntityBalanceTransferFrequency Never = new ListEntityBalanceTransferFrequency("never");

        private static readonly Dictionary <string, ListEntityBalanceTransferFrequency> _knownValues =
            new Dictionary <string, ListEntityBalanceTransferFrequency> ()
            {
                ["every-day"] = EveryDay,
                ["daily"] = Daily,
                ["every-monday"] = EveryMonday,
                ["every-tuesday"] = EveryTuesday,
                ["every-wednesday"] = EveryWednesday,
                ["every-thursday"] = EveryThursday,
                ["every-friday"] = EveryFriday,
                ["monthly"] = Monthly,
                ["revenue-day"] = RevenueDay,
                ["never"] = Never
            };

        private static readonly ConcurrentDictionary<string, ListEntityBalanceTransferFrequency> _values =
            new ConcurrentDictionary<string, ListEntityBalanceTransferFrequency>(_knownValues);

        private ListEntityBalanceTransferFrequency(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static ListEntityBalanceTransferFrequency Of(string value)
        {
            return _values.GetOrAdd(value, _ => new ListEntityBalanceTransferFrequency(value));
        }

        public static implicit operator ListEntityBalanceTransferFrequency(string value) => Of(value);
        public static implicit operator string(ListEntityBalanceTransferFrequency listentitybalancetransferfrequency) => listentitybalancetransferfrequency.Value;

        public static ListEntityBalanceTransferFrequency[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as ListEntityBalanceTransferFrequency);

        public bool Equals(ListEntityBalanceTransferFrequency? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }
}