//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// @generated-id: 3cb3c1711803
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Components
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;

    /// <summary>
    /// The frequency with which the available amount on the balance will be settled to the configured transfer<br/>
    /// destination.<br/>
    /// <br/>
    /// Settlements created during weekends or on bank holidays will take place on the next business day.
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class EntityBalanceTransferFrequency : IEquatable<EntityBalanceTransferFrequency>
    {
        public static readonly EntityBalanceTransferFrequency EveryDay = new EntityBalanceTransferFrequency("every-day");
        public static readonly EntityBalanceTransferFrequency Daily = new EntityBalanceTransferFrequency("daily");
        public static readonly EntityBalanceTransferFrequency EveryMonday = new EntityBalanceTransferFrequency("every-monday");
        public static readonly EntityBalanceTransferFrequency EveryTuesday = new EntityBalanceTransferFrequency("every-tuesday");
        public static readonly EntityBalanceTransferFrequency EveryWednesday = new EntityBalanceTransferFrequency("every-wednesday");
        public static readonly EntityBalanceTransferFrequency EveryThursday = new EntityBalanceTransferFrequency("every-thursday");
        public static readonly EntityBalanceTransferFrequency EveryFriday = new EntityBalanceTransferFrequency("every-friday");
        public static readonly EntityBalanceTransferFrequency Monthly = new EntityBalanceTransferFrequency("monthly");
        public static readonly EntityBalanceTransferFrequency RevenueDay = new EntityBalanceTransferFrequency("revenue-day");
        public static readonly EntityBalanceTransferFrequency Never = new EntityBalanceTransferFrequency("never");

        private static readonly Dictionary <string, EntityBalanceTransferFrequency> _knownValues =
            new Dictionary <string, EntityBalanceTransferFrequency> ()
            {
                ["every-day"] = EveryDay,
                ["daily"] = Daily,
                ["every-monday"] = EveryMonday,
                ["every-tuesday"] = EveryTuesday,
                ["every-wednesday"] = EveryWednesday,
                ["every-thursday"] = EveryThursday,
                ["every-friday"] = EveryFriday,
                ["monthly"] = Monthly,
                ["revenue-day"] = RevenueDay,
                ["never"] = Never
            };

        private static readonly ConcurrentDictionary<string, EntityBalanceTransferFrequency> _values =
            new ConcurrentDictionary<string, EntityBalanceTransferFrequency>(_knownValues);

        private EntityBalanceTransferFrequency(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static EntityBalanceTransferFrequency Of(string value)
        {
            return _values.GetOrAdd(value, _ => new EntityBalanceTransferFrequency(value));
        }

        public static implicit operator EntityBalanceTransferFrequency(string value) => Of(value);
        public static implicit operator string(EntityBalanceTransferFrequency entitybalancetransferfrequency) => entitybalancetransferfrequency.Value;

        public static EntityBalanceTransferFrequency[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as EntityBalanceTransferFrequency);

        public bool Equals(EntityBalanceTransferFrequency? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }
}