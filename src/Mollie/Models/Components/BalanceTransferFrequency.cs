//------------------------------------------------------------------------------
// <auto-generated>
// This code was generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
//
// Changes to this file may cause incorrect behavior and will be lost when
// the code is regenerated.
// </auto-generated>
//------------------------------------------------------------------------------
#nullable enable
namespace Mollie.Models.Components
{
    using Mollie.Utils;
    using Newtonsoft.Json;
    using System;
    using System.Collections.Concurrent;
    using System.Collections.Generic;
    using System.Linq;
    
    /// <summary>
    /// The frequency with which the available amount on the balance will be settled to the configured transfer<br/>
    /// 
    /// <remarks>
    /// destination.<br/>
    /// <br/>
    /// Settlements created during weekends or on bank holidays will take place on the next business day.
    /// </remarks>
    /// </summary>
    [JsonConverter(typeof(OpenEnumConverter))]
    public class BalanceTransferFrequency : IEquatable<BalanceTransferFrequency>
    {
        public static readonly BalanceTransferFrequency Daily = new BalanceTransferFrequency("daily");
        public static readonly BalanceTransferFrequency EveryMonday = new BalanceTransferFrequency("every-monday");
        public static readonly BalanceTransferFrequency EveryTuesday = new BalanceTransferFrequency("every-tuesday");
        public static readonly BalanceTransferFrequency EveryWednesday = new BalanceTransferFrequency("every-wednesday");
        public static readonly BalanceTransferFrequency EveryThursday = new BalanceTransferFrequency("every-thursday");
        public static readonly BalanceTransferFrequency EveryFriday = new BalanceTransferFrequency("every-friday");
        public static readonly BalanceTransferFrequency Monthly = new BalanceTransferFrequency("monthly");
        public static readonly BalanceTransferFrequency Never = new BalanceTransferFrequency("never");

        private static readonly Dictionary <string, BalanceTransferFrequency> _knownValues =
            new Dictionary <string, BalanceTransferFrequency> ()
            {
                ["daily"] = Daily,
                ["every-monday"] = EveryMonday,
                ["every-tuesday"] = EveryTuesday,
                ["every-wednesday"] = EveryWednesday,
                ["every-thursday"] = EveryThursday,
                ["every-friday"] = EveryFriday,
                ["monthly"] = Monthly,
                ["never"] = Never
            };

        private static readonly ConcurrentDictionary<string, BalanceTransferFrequency> _values =
            new ConcurrentDictionary<string, BalanceTransferFrequency>(_knownValues);

        private BalanceTransferFrequency(string value)
        {
            if (value == null) throw new ArgumentNullException(nameof(value));
            Value = value;
        }

        public string Value { get; }

        public static BalanceTransferFrequency Of(string value)
        {
            return _values.GetOrAdd(value, _ => new BalanceTransferFrequency(value));
        }

        public static implicit operator BalanceTransferFrequency(string value) => Of(value);
        public static implicit operator string(BalanceTransferFrequency balancetransferfrequency) => balancetransferfrequency.Value;

        public static BalanceTransferFrequency[] Values()
        {
            return _values.Values.ToArray();
        }

        public override string ToString() => Value.ToString();

        public bool IsKnown()
        {
            return _knownValues.ContainsKey(Value);
        }

        public override bool Equals(object? obj) => Equals(obj as BalanceTransferFrequency);

        public bool Equals(BalanceTransferFrequency? other)
        {
            if (ReferenceEquals(this, other)) return true;
            if (other is null) return false;
            return string.Equals(Value, other.Value);
        }

        public override int GetHashCode() => Value.GetHashCode();
    }

}